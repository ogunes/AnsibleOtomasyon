---

  - hosts: all
    become: true
    tasks:

    - name: Bu gorev, apt packet index'ini günceller.
      apt:
        update_cache: yes
      when: ansible_distribution == "Debian"
   
    - name: Bu gorev, apache2 yuklememesini gerceklestirir.
      apt:
        name: apache2
        state: latest
      when: ansible_distribution == "Debian"

    - name: Bu gorev, apache icin php kutuphanesini yuklememesini gerceklestirir.
      apt:
        name: libapache2-mod-php
        state: latest
      when: ansible_distribution == "Debian"